import { environment } from '../../../environments/environment';

export const LearnositySecurityCredentials = {
  consumer_key: environment.learnosity.consumerKey,
  domain: environment.learnosity.domain,
};
